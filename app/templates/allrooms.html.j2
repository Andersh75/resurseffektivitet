{% extends "headertoinclude.html.j2" %}



{% block allrooms %}
	<div class ="container">

		<h2>Salar</h2>
<br />
		<table class="table table-hover col-lg-12 col-md-12 col-sm-12 col-xs-12">

	        <tbody>
	            <tr>
	                <th class="text-left col-lg-1 col-md-1 col-sm-1 col-xs-1">Rumsnamn</th>
	                <th class="text-left col-lg-4 col-md-4 col-sm-4 col-xs-4">Antal platser</th>
					<th class="text-left col-lg-3 col-md-3 col-sm-3 col-xs-3">Rumstyp</th>
					<th class="text-left col-lg-4 col-md-4 col-sm-4 col-xs-4">Kostnad per timme</th>
	            </tr>
	            {% for item in roomsperroomtype(1) %}
		            <tr>
		                <td><a class="btn btn-primary btn-xs" href="{{ url_for('oneroom_page', roomid=item.id) }}" role="button">{{ item.code }}</a></td>
			            <td>{{ item.name }}</td>
						<td>{{ roomtypeobjectfromid(1).roomtype }}</td>
						<td>{{ roomtypeobjectfromid(1).cost }}</td>
					</tr>
	            {% endfor %}
	        </tbody>
	    </table>

	</div>
{% endblock %}
